import * as React from 'react';

export interface IDocsNavigationProps {}

export const DocsNavigation: React.FunctionComponent<IDocsNavigationProps> = (props: React.PropsWithChildren<IDocsNavigationProps>) => {
  return (
    <>

    </>
  );
};